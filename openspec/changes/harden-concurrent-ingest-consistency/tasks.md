## 1. Storage Consistency

- [x] 1.1 Serialize `ChromemStore.Upsert` executions within one store instance.
- [x] 1.2 Add a regression test for overlapping upserts against the same store.

## 2. Verification

- [x] 2.1 Run tests, vet, lint, and strict OpenSpec validation for the ingest consistency change.
