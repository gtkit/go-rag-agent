# gosec 排除规则（每条都必须写明为什么在本包不成立）
# G204：NewCommandDocumentConverter / PDFOCRBridge / ImageTextBridge / 有道笔记桥接的职责就是
#       执行调用方在 Config 里显式配置的本地命令，命令与参数来自配置而非请求；子进程使用变量是设计本身。
# G304：文档加载器、sidecar 元数据、评测报告读取的文件路径来自调用方传入的知识源与报告路径，
#       读取任意指定文件是 SDK 的核心功能，路径边界由 AccessBoundary 与接入方负责。
# G404：provider 重试退避的抖动只用于打散重试时间，不承担任何安全职责，无需密码学随机数。
GOSEC_EXCLUDE = G204,G304,G404
