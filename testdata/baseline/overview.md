# SDK Positioning Overview

go-rag-agent is an embeddable Go RAG runtime.

Use it when you want to add retrieval, citations, session memory, and traceable execution into an existing Go service without starting a separate control plane.
